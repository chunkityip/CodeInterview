package Leetcode.Company.Accentu;

import java.util.Scanner;

/*
public static void maxInArray(int arr[], int length) {
            int maxValue = Integer.MIN_VALUE; // Initialize to the smallest possible value
            int index = 0;

            for (int i = 0; i < arr.length; i++) {
                // Using Math.max() to compare values
                maxValue = Math.max(maxValue, arr[i]);

                // Update the index if the current element is greater than the current maxValue
                if (arr[i] == maxValue) {
                    index = i;
                }
            }

            System.out.println("Max Value: " + maxValue);
            System.out.println("Index: " + index);
        }

        public static void main (String[]args){
            Scanner sc = new Scanner(System.in);
            int n = sc.nextInt();
            int arr[] = new int[n];

            for (int i = 0; i < n; i++) {
                arr[i] = sc.nextInt();
            }

            maxInArray(arr, arr.length);
        }
 */




