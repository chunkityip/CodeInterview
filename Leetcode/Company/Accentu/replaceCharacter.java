package Leetcode.Company.Accentu;

import java.util.Scanner;

/*
public static void replaceChar(String str , char c1 , char c2) {
       public static void replaceCharacter(String arr, char ch1, char ch2) {
        StringBuilder sb = new StringBuilder();

        for (int i = 0; i < arr.length(); i++) {
            if (arr.charAt(i) == ch1) {
                sb.append(ch2);
            } else if (arr.charAt(i) == ch2) {
                sb.append(ch1);
            } else {
                sb.append(arr.charAt(i));
            }
        }

        System.out.println(sb.toString());
    }

    public static void main (String[] args) {
        Scanner sc = new Scanner(System.in);
        String arr = sc.next();
        char ch1 = sc.next().charAt(0);
        char ch2 = sc.next().charAt(0);

        replaceCharacter(arr , ch1 , ch2);
    }
 */

/*
You are given a function,


The function accepts a string  ‘ str’ of length n and two characters ‘ch1’ and ‘ch2’ as its arguments . Implement the function
 to modify and return the string ‘ str’ in such a way that all occurrences of ‘ch1’ in original string are replaced by ‘ch2’
  and all occurrences of ‘ch2’  in original string are replaced by ‘ch1’.

Assumption: String Contains only lower-case alphabetical letters.

Note:

Return null if string is null.
If both characters are not present in string or both of them are same , then return the string unchanged.
Example:

Input:
Str: apples
ch1:a
ch2:p
Output:
paales
Explanation:

‘A’ in original string is replaced with ‘p’ and ‘p’ in original string is replaced with ‘a’, thus output is paales.
 */

public class replaceCharacter {



}
