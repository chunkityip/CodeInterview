package Leetcode.Company.Accentu;

import java.util.Scanner;

/*
public class checkPalindrome {

    public class PalindromeNumbers {
        // Function to reverse a number
        static int reverse(int num) {
            int reversedNum = 0;
            while (num != 0) {
                int r = num % 10;
                reversedNum = (reversedNum * 10) + r;
                num = num / 10;
            }
            return reversedNum;
        }

        public static void main(String[] args) {
            Scanner sc = new Scanner(System.in);
            System.out.println("Enter Lower and Upper Limit");
            int ll = sc.nextInt();
            int ul = sc.nextInt();

            if (ll > ul) {
                System.out.println("Invalid range. Lower limit should be less than or equal to upper limit.");
                return;
            }

            System.out.println("Palindrome numbers between " + ll + " and " + ul + " are:");

            for (int i = ll; i <= ul; i++) {
                if (i == reverse(i)) {
                    System.out.print(i + " ");
                }
            }
        }
    }
    */

/*
        Instructions: You are required to write the code. You can click on compile and run anytime to check compilation/execution status. The code should be logically/syntactically correct.

        Question: Write a program in C such that it takes a lower limit and upper limit as inputs and print all the intermediate palindrome numbers.

        Test Cases:

        TestCase 1:
        Input :
        10 , 80
        Expected Result:
        11 , 22 , 33 , 44 , 55 , 66 , 77.

        Test Case 2:
        Input:
        100,200
        Expected Result:
        101 , 111 , 121 , 131 , 141 , 151 , 161 , 171 , 181 , 191.
  */

